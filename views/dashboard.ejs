<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles.css" />
    <title>Dashboard</title>
    <style>
      body {
        font-family: "Arial", sans-serif;
        background: linear-gradient(135deg, #d1e9ff, #fff);
        margin: 0;
        padding: 0;
        color: #333;
      }

      header {
        background-color: #007bff;
        color: white;
        padding: 20px;
        text-align: center;
        font-size: 2.5em;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
      }

      .container {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: 50px 20px;
        text-align: center;
      }

      h1 {
        font-size: 2em;
        margin: 20px 0;
        color: #343a40;
      }

      ul {
        list-style-type: none;
        padding: 0;
        margin: 30px 0;
        width: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
      }

      ul li {
        margin: 15px 0;
      }

      ul li a {
        text-decoration: none;
        font-size: 1.2em;
        color: #fff;
        background-color: #28a745;
        padding: 15px 30px;
        border-radius: 30px;
        transition: background-color 0.3s, transform 0.3s;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
      }

      ul li a:hover {
        background-color: #218838;
        transform: translateY(-2px);
      }

      h2 {
        font-size: 1.5em;
        color: #dc3545;
        margin-top: 30px;
      }

      .notifications {
        background-color: #f8d7da;
        padding: 20px;
        border-radius: 8px;
        margin-bottom: 20px;
        border-left: 5px solid #dc3545;
        width: 100%;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        text-align: left;
      }

      .notification-message {
        margin: 0;
      }

      .back-to-login {
        display: inline-block;
        margin-top: 30px;
        padding: 12px 25px;
        background-color: #007bff;
        color: #fff;
        text-decoration: none;
        border-radius: 30px;
        font-size: 1.2em;
        transition: background-color 0.3s, transform 0.3s;
      }

      .back-to-login:hover {
        background-color: #0056b3;
        transform: translateY(-2px);
      }

      /* Responsive styles */
      @media (max-width: 600px) {
        h1 {
          font-size: 1.5em;
        }

        ul li a {
          padding: 10px 20px;
          font-size: 1em;
        }
      }
    </style>
  </head>
  <body>
    <header>Welcome to Your Dashboard</header>
    <div class="container">
      <h1>Hello, <%= user.Name %>!</h1>
      <ul>
        <li><a href="/meals/update">Update Meal</a></li>
        <li><a href="/meals/history">View Meal History</a></li>
      </ul>

      <% if (notifications && notifications.length > 0) { %>
      <h2>Recent Notifications</h2>
      <div class="notifications">
        <ul>
          <% notifications.forEach(notification => { %>
          <li class="notification-message"><%= notification.message %></li>
          <% }); %>
        </ul>
      </div>
      <% } %>

      <!-- Make sure this link points to the correct login route -->
      <a href="/users/login" class="back-to-login">Back to Login</a>
    </div>
  </body>
</html>
